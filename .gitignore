node_modules/
dist/
build/
logs/

# Variáveis de ambiente — todos ignorados exceto o exemplo
.env
.env.*
!.env.example

# Firebase Admin SDK — credenciais da service account (nunca commitar)
service-account.json

# Uploads — ignora arquivos mas mantém a pasta via .gitkeep
public/uploads/covers/*
!public/uploads/covers/.gitkeep
